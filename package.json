{
  "name": "webinarjam-beehiiv-bridge",
  "version": "1.0.0",
  "description": "Cloudflare Worker that forwards WebinarJam registrants into Beehiiv (create subscriber + enroll in automation).",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
